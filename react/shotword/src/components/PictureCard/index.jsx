import './style.css'

const PictureCard = (props) => {
    const {
        audio,
        word,
        uploadImg,
    } = props
    return (

        <div>
            PictureCard
        </div>
    )
}

export default PictureCard;